mode: ContinuousDeployment
tag-prefix: '[vV]'
branches:
  main:
    regex: ^main$
    increment: Patch
    is-release-branch: true
  feature:
    regex: ^features?[/-]
    increment: Inherit
    source-branches: ['main']